---
- name: Prepare
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Refreshing the apt cache the role's apt task assumes is present"
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
