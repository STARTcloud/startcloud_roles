---
argument_specs:
  main:
    short_description: Install and configure the zrepl ZFS replication daemon
    description:
      - Adds the upstream zrepl package repository and installs the zrepl ZFS replication daemon.
    author: Mark Gilbert
    options:
      run_tasks:
        type: bool
        default: true
        description: Master gate — when false the role loads its vars but runs no tasks.
