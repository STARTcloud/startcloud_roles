---
- name: Prepare
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Refreshing the apt cache so the exim4 packages are resolvable"
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 600
      changed_when: false
