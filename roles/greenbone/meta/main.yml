---
galaxy_info:
  role_name: greenbone
  author: Mark Gilbert
  description: Deploys single-node Greenbone Community Edition (GVM / OpenVAS) using the official Greenbone Community Containers via Docker Compose.
  company: STARTcloud
  issue_tracker_url: https://github.com/STARTcloud/startcloud_roles/issues
  license: Apache-2.0
  min_ansible_version: '2.15'
  platforms:
    - name: Debian
      versions: ['bookworm', 'trixie']
    - name: Ubuntu
      versions: ['jammy', 'noble']
  galaxy_tags:
    - greenbone
    - gvm
    - openvas
    - security
    - scanner
dependencies:
  - role: startcloud.startcloud_roles.docker
collections:
  - startcloud.startcloud_roles
