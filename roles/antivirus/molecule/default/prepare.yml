---
- name: Prepare
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Refreshing the apt cache the base image ships stale"
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 0
      changed_when: false
