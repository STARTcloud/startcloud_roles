---
argument_specs:
  main:
    short_description: SikuliX visual GUI automation installation
    description:
      - Installs the SikuliX visual GUI automation tool along with its OpenCV, JRuby, and Jython dependencies and desktop launchers on Debian-based systems.
    author: Mark Gilbert
    options:
      run_tasks:
        type: bool
        default: true
        description: Master gate — when false the role loads its vars but runs no tasks.
