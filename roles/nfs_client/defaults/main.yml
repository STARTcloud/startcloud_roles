---
run_tasks: true

## List of NFS shares to mount. Driven from Hosts.yml. Empty = no-op.
## Each item:
##   src:    "server:/export/path"   (required)
##   path:   "/local/mount/point"    (required)
##   fstype: nfs                      (optional, default nfs)
##   opts:   "defaults,_netdev"       (optional, default defaults,_netdev)
##   state:  mounted                  (optional, default mounted)
nfs_client_mounts: []
