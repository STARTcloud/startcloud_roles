---
galaxy_info:
  role_name: telegraf
  author: Mark Gilbert
  description: Installs and configures the Telegraf metrics agent on Debian-family systems and FreeBSD, natively or in Docker; owns /etc/telegraf/telegraf.conf as the canonical metrics role.
  company: STARTcloud
  issue_tracker_url: https://github.com/STARTcloud/startcloud_roles/issues
  license: GPL-2.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: ["bookworm", "trixie"]
    - name: Ubuntu
      versions: ["jammy", "noble"]
    - name: FreeBSD
      versions: ["14.0"]
  galaxy_tags:
    - monitoring
    - telegraf
    - metrics
    - influxdb
dependencies: []
collections:
  - startcloud.startcloud_roles
