---
- name: Prepare
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Refreshing the apt cache so the role can resolve its dependencies"
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      changed_when: false
