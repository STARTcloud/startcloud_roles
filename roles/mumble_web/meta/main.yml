---
galaxy_info:
  role_name: mumble_web
  author: Mark Gilbert
  description: Deploys the mumble-web HTML5 client and its WebSocket/WebRTC proxy as Docker containers, bridging browsers to a Mumble (murmur) server.
  company: STARTcloud
  issue_tracker_url: https://github.com/STARTcloud/startcloud_roles/issues
  license: GPL-2.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: ["bookworm", "trixie"]
    - name: Ubuntu
      versions: ["jammy", "noble"]
  galaxy_tags:
    - mumble
    - voip
    - docker
    - webclient
    - websocket
dependencies:
  - role: startcloud.startcloud_roles.docker
collections:
  - startcloud.startcloud_roles
