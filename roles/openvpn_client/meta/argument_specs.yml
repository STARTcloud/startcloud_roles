---
argument_specs:
  main:
    short_description: Configure an OpenVPN client
    description:
      - Deploys the OpenVPN client configuration, installs the CA, certificate, and key material, and enables the OpenVPN service.
    author: Mark Gilbert
    options:
      run_tasks:
        type: bool
        default: true
        description: Master gate — when false the role loads its vars but runs no tasks.
