---
argument_specs:
  main:
    short_description: Installs and configures the Squid caching web proxy.
    description:
      - Installs the Squid package and renders its configuration from a template.
      - Validates the configuration with squid -k parse and restarts the service when the configuration changes.
    author: Mark Gilbert
    options:
      run_tasks:
        type: bool
        default: true
        description: Master gate — when false the role loads its vars but runs no tasks.
      squid_packages:
        type: list
        elements: str
        default:
          - squid
        description: Packages to install for the Squid proxy.
      proxy_port:
        type: int
        default: 3128
        description: TCP port Squid listens on; rendered into the http_port directive of squid.conf.
