---
- name: Prepare
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Updating the apt cache the base image ships empty"
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
