---
- name: "Restarting kubelet"
  ansible.builtin.systemd:
    name: kubelet
    state: restarted
    daemon_reload: true

- name: "Making Kubernetes yum cache"
  ansible.builtin.dnf:
    update_cache: true
    disablerepo: "*"
    enablerepo: kubernetes
